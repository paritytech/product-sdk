# Dependencies
node_modules/

# Build outputs
dist/
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Test coverage
coverage/

# Package manager locks
package-lock.json
yarn.lock

#local docs
local-docs

# Bundle bench: ephemeral outputs, baseline JSON is committed.
product-sdk/bundle-size.md
product-sdk/bundle-size.base.json
product-sdk/bundle-size.head.json
product-sdk/bundle-size.diff.md

# AI session/per-developer state (keep .claude/commands, .claude/skills,
# .claude/agents, CLAUDE.md, .claude-plugin/ — those are intentional team config)
.claude/sessions/
.claude/cache/
.claude/settings.local.json