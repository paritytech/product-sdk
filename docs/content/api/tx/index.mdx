---
generated: true
title: "@parity/product-sdk-tx"
---

# `@parity/product-sdk-tx`

```sh npm2yarn
npm install @parity/product-sdk-tx
```

## Classes

| Name | Summary |
| --- | --- |
| [`TxAccountMappingError`](/api/tx/tx-account-mapping-error) | Error thrown when account mapping fails. |
| [`TxBatchError`](/api/tx/tx-batch-error) | Error specific to batch transaction construction (e.g., empty calls array). |
| [`TxDispatchError`](/api/tx/tx-dispatch-error) | The transaction was included on-chain but the dispatch failed. |
| [`TxDryRunError`](/api/tx/tx-dry-run-error) | A dry-run simulation failed before the transaction was submitted on-chain. |
| [`TxError`](/api/tx/tx-error) | Base class for all transaction errors. Use `instanceof TxError` to catch any tx-related error. |
| [`TxSigningRejectedError`](/api/tx/tx-signing-rejected-error) | The user rejected the signing request in their wallet. |
| [`TxTimeoutError`](/api/tx/tx-timeout-error) | The transaction did not finalize within the configured timeout. It may still be processing on-chain. |

## Functions

| Name | Summary |
| --- | --- |
| [`applyWeightBuffer()`](/api/tx/apply-weight-buffer) | Apply a safety buffer to weight estimates from a dry-run result. |
| [`batchSubmitAndWatch()`](/api/tx/batch-submit-and-watch) | Batch multiple transactions into a single Substrate Utility batch and submit. |
| [`calculateDelay()`](/api/tx/calculate-delay) | Calculate delay with exponential backoff and jitter. |
| [`createDevSigner()`](/api/tx/create-dev-signer) | Create a PolkadotSigner for a standard Substrate dev account. |
| [`ensureAccountMapped()`](/api/tx/ensure-account-mapped) | Ensure an account's SS58 address is mapped to its H160 EVM address on-chain. |
| [`extractTransaction()`](/api/tx/extract-transaction) | Validate an Ink SDK dry-run result and extract the submittable transaction. |
| [`formatDispatchError()`](/api/tx/format-dispatch-error) | Extract a human-readable error from a transaction result's dispatch error. |
| [`formatDryRunError()`](/api/tx/format-dry-run-error) | Extract a human-readable error from a failed dry-run result. |
| [`getDevPublicKey()`](/api/tx/get-dev-public-key) | Get the public key bytes for a dev account. |
| [`isAccountMapped()`](/api/tx/is-account-mapped) | Check if an address is mapped on-chain. |
| [`isSigningRejection()`](/api/tx/is-signing-rejection) | Check if an error looks like a user-rejected signing request. |
| [`submitAndWatch()`](/api/tx/submit-and-watch) | Submit a transaction and watch its lifecycle through signing, broadcasting, |
| [`withRetry()`](/api/tx/with-retry) | Wrap an async function with retry logic and exponential backoff. |

## Interfaces

| Name | Summary |
| --- | --- |
| [`BatchApi`](/api/tx/batch-api) | Minimal structural type for a PAPI typed API with the Utility pallet. |
| [`BatchSubmitOptions`](/api/tx/batch-submit-options) | Options for `batchSubmitAndWatch`. Extends `SubmitOptions` with batch mode. |
| [`EnsureAccountMappedOptions`](/api/tx/ensure-account-mapped-options) | Options for `ensureAccountMapped`. |
| [`MappingChecker`](/api/tx/mapping-checker) | Minimal interface for checking if an address is mapped on-chain. |
| [`RetryOptions`](/api/tx/retry-options) | Options for `withRetry`. |
| [`ReviveApi`](/api/tx/revive-api) | Minimal typed API shape for `Revive.map_account()`. |
| [`SubmitOptions`](/api/tx/submit-options) | Options for `submitAndWatch`. |
| [`SubmittableTransaction`](/api/tx/submittable-transaction) | Structural type for any transaction object that supports Observable-based |
| [`TxResult`](/api/tx/tx-result) | Successful transaction result. |
| [`Weight`](/api/tx/weight) | Substrate weight representing computational and storage resources. |

## Type Aliases

| Name | Summary |
| --- | --- |
| [`BatchableCall`](/api/tx/batchable-call) | A transaction or decoded call that can be included in a batch. |
| [`BatchMode`](/api/tx/batch-mode) | Batch execution mode corresponding to Substrate's Utility pallet. |
| [`DevAccountName`](/api/tx/dev-account-name) | Standard Substrate dev account names. |
| [`TxEvent`](/api/tx/tx-event) | PAPI transaction event (discriminated union). |
| [`TxStatus`](/api/tx/tx-status) | Transaction lifecycle status for UI callbacks. |
| [`WaitFor`](/api/tx/wait-for) | When to resolve the submission promise. |
