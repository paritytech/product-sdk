---
generated: true
title: "interface SubmittableTransaction"
description: "Structural type for any transaction object that supports Observable-based"
---
# `interface SubmittableTransaction`

Structural type for any transaction object that supports Observable-based
sign-submit-and-watch. Works with raw PAPI transactions and Ink SDK
resolved transactions.

## Properties

### property `decodedCall`

```ts
decodedCall: unknown
```

The decoded call data. Present on PAPI transactions.

### property `signSubmitAndWatch`

```ts
signSubmitAndWatch: (signer: PolkadotSigner, options?: { mortality?: { mortal: boolean; period: number } }) => { subscribe: (handlers: { error: (error: Error) => void; next: (event: TxEvent) => void }) => { unsubscribe: () => void } }
```

### property `waited`

```ts
waited: Promise<SubmittableTransaction>
```

Present on Ink SDK AsyncTransaction wrappers.
