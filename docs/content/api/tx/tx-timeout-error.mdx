---
generated: true
title: "class TxTimeoutError"
description: "The transaction did not finalize within the configured timeout. It may still be processing on-chain."
---
# `class TxTimeoutError`

The transaction did not finalize within the configured timeout. It may still be processing on-chain.

**Extends:** `TxError`

## Constructors

### constructor `constructor`

```ts
new TxTimeoutError(timeoutMs: number): TxTimeoutError
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `timeoutMs` | `number` | — |

## Properties

### property `timeoutMs`

```ts
timeoutMs: number
```
