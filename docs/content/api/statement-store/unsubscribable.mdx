---
generated: true
title: "interface Unsubscribable"
description: "Handle returned by subscription methods. Call `unsubscribe()` to stop receiving events."
---
# `interface Unsubscribable`

Handle returned by subscription methods. Call `unsubscribe()` to stop receiving events.

## Properties

### property `unsubscribe`

```ts
unsubscribe: () => void
```
