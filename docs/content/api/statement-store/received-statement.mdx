---
generated: true
title: "interface ReceivedStatement"
description: "A received statement with typed data and metadata."
---
# `interface ReceivedStatement`

A received statement with typed data and metadata.

## Properties

### property `channelHex`

```ts
channelHex: string
```

Channel hex, if present.

### property `data`

```ts
data: T
```

Parsed data payload.

### property `expiry`

```ts
expiry: bigint
```

Combined expiry value (upper 32 bits = timestamp, lower 32 bits = sequence).

### property `raw`

```ts
raw: Statement
```

The full statement from the transport for advanced inspection.

### property `signerHex`

```ts
signerHex: string
```

Signer's public key hex (from proof), if present.

### property `topics`

```ts
topics: string[]
```

Topics array (hex strings).
