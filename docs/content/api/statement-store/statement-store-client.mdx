---
generated: true
title: "class StatementStoreClient"
description: "High-level client for the Polkadot Statement Store."
---
# `class StatementStoreClient`

High-level client for the Polkadot Statement Store.

Provides a simple publish/subscribe API over the ephemeral statement store,
handling topic management and signing through the host API.

The SDK is designed to run exclusively inside a host container.

## Examples

```ts
import { StatementStoreClient } from "@parity/product-sdk-statement-store";

// Inside a container (host mode)
const client = new StatementStoreClient({ appName: "my-app" });
await client.connect({ mode: "host", accountId: ["5Grw...", 42] });

// Publish
await client.publish({ type: "presence", peerId: "abc" }, {
    channel: "presence/abc",
    topic2: "room-123",
});

// Subscribe
client.subscribe<{ type: string }>(statement => {
    console.log(statement.data.type);
});

// Cleanup
client.destroy();
```

## Constructors

### constructor `constructor`

```ts
new StatementStoreClient(config: StatementStoreConfig): StatementStoreClient
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `config` | `StatementStoreConfig` | — |

## Methods

### method `connect`

Connect to the statement store and start receiving statements.

#### Overload 1

```ts
connect(credentials: ConnectionCredentials): Promise<void>
```

Connect to the statement store and start receiving statements.

##### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `credentials` | `ConnectionCredentials` | Connection credentials (host accountId or local signer). |

##### Returns

`Promise<void>`

##### Throws

- If the transport cannot be established.

#### Overload 2

```ts
connect(signer: StatementSignerWithKey): Promise<void>
```

##### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `signer` | `StatementSignerWithKey` | — |

##### Returns

`Promise<void>`


### method `destroy`

Destroy the client, unsubscribing and closing the transport.

Safe to call multiple times. After destruction, the client cannot be reused.

```ts
destroy(): void
```

#### Parameters

_No parameters._

### method `getPublicKeyHex`

Get the signer's public key as a hex string (with 0x prefix).

```ts
getPublicKeyHex(): string
```

#### Parameters

_No parameters._

#### Returns

`string` — The hex-encoded public key, or empty string if not connected or in host mode.

### method `isConnected`

Whether the client is connected and ready to publish/subscribe.

```ts
isConnected(): boolean
```

#### Parameters

_No parameters._

#### Returns

`boolean`

### method `publish`

Publish typed data to the statement store.

```ts
publish(data: T, options?: PublishOptions): Promise<boolean>
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `T` | The value to publish (must be JSON-serializable, max 512 bytes). |
| `options` _(optional)_ | `PublishOptions` | Optional channel, topic2, TTL, and decryption key overrides. |

#### Returns

`Promise<boolean>` — `true` if accepted, `false` if rejected or errored.

#### Throws

- If not connected.
- If the encoded data exceeds 512 bytes.

### method `subscribe`

Subscribe to incoming statements on this application's topic.

```ts
subscribe(callback: (statement: ReceivedStatement<T>) => void, options?: { topic2?: string }): Unsubscribable
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `callback` | `(statement: ReceivedStatement<T>) => void` | Called for each new statement. |
| `options` _(optional)_ | `{ topic2?: string }` | Optional secondary topic filter. |

#### Returns

`Unsubscribable` — A handle to unsubscribe.
