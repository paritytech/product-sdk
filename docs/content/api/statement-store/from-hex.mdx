---
generated: true
title: "fromHex()"
description: "Convert a hex string (with or without 0x prefix) to bytes."
---
# `fromHex()`

Convert a hex string (with or without 0x prefix) to bytes.

```ts
fromHex(hex: string): Uint8Array
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hex` | `string` | — |

## Returns

`Uint8Array`
