---
generated: true
title: "topicToHex()"
description: "Convert a topic or channel hash to a hex string (with 0x prefix)."
---
# `topicToHex()`

Convert a topic or channel hash to a hex string (with 0x prefix).

```ts
topicToHex(hash: Uint8Array): string
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hash` | `Uint8Array` | A 32-byte topic or channel hash. |

## Returns

`string` — Hex string with "0x" prefix.
