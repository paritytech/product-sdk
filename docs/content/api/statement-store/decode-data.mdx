---
generated: true
title: "decodeData()"
description: "Decode a JSON-serialized data payload from statement bytes."
---
# `decodeData()`

Decode a JSON-serialized data payload from statement bytes.

```ts
decodeData(bytes: Uint8Array): T
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bytes` | `Uint8Array` | UTF-8 encoded JSON bytes. |

## Returns

`T` — The parsed value.

## Throws

- If the bytes are not valid UTF-8 or valid JSON.
