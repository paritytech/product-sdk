---
generated: true
title: "toHex()"
description: "Convert bytes to a hex string with 0x prefix."
---
# `toHex()`

Convert bytes to a hex string with 0x prefix.

```ts
toHex(bytes: Uint8Array): string
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bytes` | `Uint8Array` | — |

## Returns

`string`
