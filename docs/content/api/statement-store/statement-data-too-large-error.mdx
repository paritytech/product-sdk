---
generated: true
title: "class StatementDataTooLargeError"
description: "The statement data payload exceeds the maximum allowed size."
---
# `class StatementDataTooLargeError`

The statement data payload exceeds the maximum allowed size.

The statement store protocol limits individual statement data
to `MAX_STATEMENT_SIZE` bytes (512 bytes).

**Extends:** `StatementStoreError`

## Constructors

### constructor `constructor`

```ts
new StatementDataTooLargeError(actualSize: number, maxSize: number = MAX_STATEMENT_SIZE): StatementDataTooLargeError
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `actualSize` | `number` | — |
| `maxSize` | `number` = `MAX_STATEMENT_SIZE` | — |

## Properties

### property `actualSize`

```ts
actualSize: number
```

The actual size of the data in bytes.

### property `maxSize`

```ts
maxSize: number
```

The maximum allowed size in bytes.
