---
generated: true
title: "ss58ToH160()"
description: "Convert an SS58 address to its H160 EVM address."
---
# `ss58ToH160()`

Convert an SS58 address to its H160 EVM address.

Handles both native Substrate accounts (keccak256 path) and
EVM-derived accounts (0xEE padding strip).

```ts
ss58ToH160(address: string): `0x${string}`
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `address` | `string` | — |

## Returns

``0x${string}``
