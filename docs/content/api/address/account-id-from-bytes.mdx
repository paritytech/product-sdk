---
generated: true
title: "accountIdFromBytes()"
description: "Encode an SS58 address from a 32-byte public key using polkadot-api's AccountId codec."
---
# `accountIdFromBytes()`

Encode an SS58 address from a 32-byte public key using polkadot-api's AccountId codec.
This is the inverse of `accountIdBytes()`.

```ts
accountIdFromBytes(publicKey: Uint8Array, prefix: number = GENERIC_PREFIX): SS58String
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `publicKey` | `Uint8Array` | — |
| `prefix` | `number` = `GENERIC_PREFIX` | — |

## Returns

`SS58String`
