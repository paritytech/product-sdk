---
generated: true
title: "normalizeSs58()"
description: "Re-encode an SS58 address with a different network prefix."
---
# `normalizeSs58()`

Re-encode an SS58 address with a different network prefix.
Returns null if the input is not a valid SS58 address.

```ts
normalizeSs58(address: string, prefix: number = GENERIC_PREFIX): SS58String | null
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `address` | `string` | — |
| `prefix` | `number` = `GENERIC_PREFIX` | — |

## Returns

`SS58String | null`
