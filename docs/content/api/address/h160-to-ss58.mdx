---
generated: true
title: "h160ToSs58()"
description: "Convert an H160 EVM address to its corresponding SS58 address."
---
# `h160ToSs58()`

Convert an H160 EVM address to its corresponding SS58 address.

Constructs an "EVM-derived" AccountId32 by padding the H160 with 0xEE bytes.
These accounts are implicitly mapped in pallet-revive.

```ts
h160ToSs58(evmAddress: string, prefix: number = 42): SS58String
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `evmAddress` | `string` | — |
| `prefix` | `number` = `42` | — |

## Returns

`SS58String`
