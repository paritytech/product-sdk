---
generated: true
title: "accountIdBytes()"
description: "Decode an SS58 address to its 32-byte AccountId using polkadot-api's AccountId codec."
---
# `accountIdBytes()`

Decode an SS58 address to its 32-byte AccountId using polkadot-api's AccountId codec.
This is the inverse of `accountIdFromBytes()`.

```ts
accountIdBytes(address: string): Uint8Array
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `address` | `string` | — |

## Returns

`Uint8Array`
