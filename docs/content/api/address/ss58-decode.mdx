---
generated: true
title: "ss58Decode()"
description: "Decode an SS58 address into its raw public key bytes and network prefix."
---
# `ss58Decode()`

Decode an SS58 address into its raw public key bytes and network prefix.

```ts
ss58Decode(address: string): { prefix: number; publicKey: Uint8Array }
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `address` | `string` | — |

## Returns

`{ prefix: number; publicKey: Uint8Array }`
