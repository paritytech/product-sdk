---
generated: true
title: "interface BulletinApi"
description: "Bulletin Chain API exposed by the SDK"
---
# `interface BulletinApi`

Bulletin Chain API exposed by the SDK

## Methods

### method `computeCid`

Compute CID for data without uploading

```ts
computeCid(data: string | Uint8Array<ArrayBufferLike>): string
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `string \| Uint8Array<ArrayBufferLike>` | — |

#### Returns

`string`

### method `fetch`

Fetch data by CID

```ts
fetch(cid: string): Promise<Uint8Array<ArrayBufferLike>>
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `cid` | `string` | — |

#### Returns

`Promise<Uint8Array<ArrayBufferLike>>`

### method `upload`

Upload data to Bulletin Chain

```ts
upload(data: string | Uint8Array<ArrayBufferLike>): Promise<string>
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `string \| Uint8Array<ArrayBufferLike>` | — |

#### Returns

`Promise<string>`
