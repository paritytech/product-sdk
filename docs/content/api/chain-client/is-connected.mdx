---
generated: true
title: "isConnected()"
description: "Check if a chain is currently connected."
---
# `isConnected()`

Check if a chain is currently connected.

Synchronous — no side effects, no initialization.

```ts
isConnected(descriptor: ChainDefinition): boolean
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `descriptor` | `ChainDefinition` | — |

## Returns

`boolean`
