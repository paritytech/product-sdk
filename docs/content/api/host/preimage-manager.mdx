---
generated: true
title: "interface PreimageManager"
description: "Preimage manager interface for bulletin chain operations."
---
# `interface PreimageManager`

Preimage manager interface for bulletin chain operations.

## Methods

### method `lookup`

Look up a preimage by key.

```ts
lookup(key: string, callback: (preimage: Uint8Array<ArrayBufferLike> | null) => void): { onInterrupt: (cb: () => void) => () => void; unsubscribe: () => void }
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `key` | `string` | The preimage key (hex string). |
| `callback` | `(preimage: Uint8Array<ArrayBufferLike> \| null) => void` | Called with the data when found, or null if not yet available. |

#### Returns

`{ onInterrupt: (cb: () => void) => () => void; unsubscribe: () => void }` — Subscription handle with unsubscribe method.

### method `submit`

Submit a preimage to the bulletin chain.

```ts
submit(data: Uint8Array): Promise<string>
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `Uint8Array` | The data to submit. |

#### Returns

`Promise<string>` — The preimage key (hex string).
