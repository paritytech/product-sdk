---
generated: true
title: "getAccountsProvider()"
description: "Get the accounts provider for managing host accounts."
---
# `getAccountsProvider()`

Get the accounts provider for managing host accounts.

```ts
getAccountsProvider(): Promise<AccountsProvider | null>
```

## Parameters

_No parameters._

## Returns

`Promise<AccountsProvider | null>` — The accounts provider, or `null` if unavailable.
