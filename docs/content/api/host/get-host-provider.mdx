---
generated: true
title: "getHostProvider()"
description: "Get a PAPI-compatible JSON-RPC provider that routes through the host connection."
---
# `getHostProvider()`

Get a PAPI-compatible JSON-RPC provider that routes through the host connection.

When running inside a Polkadot container, this wraps the chain connection via the
host's `createPapiProvider`, enabling shared connections and efficient routing.
Returns `null` when `@novasamatech/product-sdk` is unavailable.

```ts
getHostProvider(genesisHash: `0x${string}`): Promise<JsonRpcProvider | null>
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `genesisHash` | ``0x${string}`` | Genesis hash of the target chain (`0x`-prefixed hex string). |

## Returns

`Promise<JsonRpcProvider | null>` — A host-routed `JsonRpcProvider`, or `null` if unavailable.
