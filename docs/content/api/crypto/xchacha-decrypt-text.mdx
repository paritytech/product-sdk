---
generated: true
title: "xchachaDecryptText()"
description: "Decrypt XChaCha20-Poly1305 ciphertext back to a UTF-8 string."
---
# `xchachaDecryptText()`

Decrypt XChaCha20-Poly1305 ciphertext back to a UTF-8 string.

```ts
xchachaDecryptText(ciphertext: Uint8Array, key: Uint8Array, nonce: Uint8Array): string
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ciphertext` | `Uint8Array` | Data to decrypt. |
| `key` | `Uint8Array` | 32-byte encryption key. |
| `nonce` | `Uint8Array` | 24-byte nonce used during encryption. |

## Returns

`string` — The decrypted plaintext string.

## Throws

- If decryption or authentication fails.
