---
generated: true
title: "@parity/product-sdk-crypto"
description: "Cryptographic primitives for the Polkadot app ecosystem."
---

# `@parity/product-sdk-crypto`

Cryptographic primitives for the Polkadot app ecosystem.

Provides symmetric encryption (AES-256-GCM, ChaCha20-Poly1305, XChaCha20-Poly1305),
key derivation (HKDF-SHA256), asymmetric encryption (NaCl box / sealed box),
hashing (BLAKE2b-256, SHA-256, Keccak-256), and cryptographic random bytes.
All functions are synchronous and framework-agnostic.

```sh npm2yarn
npm install @parity/product-sdk-crypto
```

## Functions

| Name | Summary |
| --- | --- |
| [`aesGcmDecrypt()`](/api/crypto/aes-gcm-decrypt) | Decrypt binary data with AES-256-GCM. |
| [`aesGcmDecryptPacked()`](/api/crypto/aes-gcm-decrypt-packed) | Decrypt a packed AES-256-GCM buffer (nonce prepended to ciphertext). |
| [`aesGcmDecryptText()`](/api/crypto/aes-gcm-decrypt-text) | Decrypt AES-256-GCM ciphertext back to a UTF-8 string. |
| [`aesGcmEncrypt()`](/api/crypto/aes-gcm-encrypt) | Encrypt binary data with AES-256-GCM. |
| [`aesGcmEncryptPacked()`](/api/crypto/aes-gcm-encrypt-packed) | Encrypt binary data with AES-256-GCM, returning a single packed buffer. |
| [`aesGcmEncryptText()`](/api/crypto/aes-gcm-encrypt-text) | Encrypt a UTF-8 string with AES-256-GCM. |
| [`blake2b256()`](/api/crypto/blake2b256) | Compute a 32-byte BLAKE2b-256 hash. |
| [`boxDecrypt()`](/api/crypto/box-decrypt) | NaCl box decrypt: authenticated decryption where both sender and recipient are known. |
| [`boxEncrypt()`](/api/crypto/box-encrypt) | NaCl box encrypt: authenticated encryption where both sender and recipient are known. |
| [`bytesToHex()`](/api/crypto/bytes-to-hex) | Convert a `Uint8Array` to its lowercase hexadecimal string representation. |
| [`chachaDecrypt()`](/api/crypto/chacha-decrypt) | Decrypt binary data with ChaCha20-Poly1305 (RFC 8439). |
| [`chachaDecryptText()`](/api/crypto/chacha-decrypt-text) | Decrypt ChaCha20-Poly1305 ciphertext back to a UTF-8 string. |
| [`chachaEncrypt()`](/api/crypto/chacha-encrypt) | Encrypt binary data with ChaCha20-Poly1305 (RFC 8439). |
| [`chachaEncryptText()`](/api/crypto/chacha-encrypt-text) | Encrypt a UTF-8 string with ChaCha20-Poly1305. |
| [`concatBytes()`](/api/crypto/concat-bytes) | Concatenate multiple `Uint8Array` instances into a single `Uint8Array`. |
| [`deriveKey()`](/api/crypto/derive-key) | Derive a 32-byte key using HKDF-SHA256 (RFC 5869). |
| [`expand()`](/api/crypto/expand) | Re-export the full HKDF function from `@noble/hashes` for advanced use cases |
| [`extract()`](/api/crypto/extract) | Re-export the full HKDF function from `@noble/hashes` for advanced use cases |
| [`hexToBytes()`](/api/crypto/hex-to-bytes) | Decode a hexadecimal string into a `Uint8Array`. |
| [`keccak256()`](/api/crypto/keccak256) | Compute a 32-byte Keccak-256 hash. |
| [`randomBytes()`](/api/crypto/random-bytes) | Generate `length` cryptographically secure random bytes. |
| [`sealedBoxDecrypt()`](/api/crypto/sealed-box-decrypt) | Sealed box decrypt: extract ephemeral public key and nonce, then open with recipient's secret key. |
| [`sealedBoxEncrypt()`](/api/crypto/sealed-box-encrypt) | Sealed box encrypt: uses an ephemeral keypair so the recipient cannot identify the sender. |
| [`sha256()`](/api/crypto/sha256) | Compute a 32-byte SHA2-256 hash. |
| [`utf8ToBytes()`](/api/crypto/utf8-to-bytes) | Encode a UTF-8 string into a `Uint8Array`. |
| [`xchachaDecrypt()`](/api/crypto/xchacha-decrypt) | Decrypt binary data with XChaCha20-Poly1305. |
| [`xchachaDecryptPacked()`](/api/crypto/xchacha-decrypt-packed) | Decrypt a packed XChaCha20-Poly1305 buffer (nonce prepended to ciphertext). |
| [`xchachaDecryptText()`](/api/crypto/xchacha-decrypt-text) | Decrypt XChaCha20-Poly1305 ciphertext back to a UTF-8 string. |
| [`xchachaEncrypt()`](/api/crypto/xchacha-encrypt) | Encrypt binary data with XChaCha20-Poly1305. |
| [`xchachaEncryptPacked()`](/api/crypto/xchacha-encrypt-packed) | Encrypt binary data with XChaCha20-Poly1305, returning a single packed buffer. |
| [`xchachaEncryptText()`](/api/crypto/xchacha-encrypt-text) | Encrypt a UTF-8 string with XChaCha20-Poly1305. |

## Interfaces

| Name | Summary |
| --- | --- |
| [`EncryptedPayload`](/api/crypto/encrypted-payload) | Common encrypted payload envelope carrying algorithm metadata alongside ciphertext. |
| [`nacl`](/api/crypto/nacl) | — |

## Type Aliases

| Name | Summary |
| --- | --- |
| [`KemAlgorithm`](/api/crypto/kem-algorithm) | Key encapsulation mechanism identifiers. |
| [`SymmetricAlgorithm`](/api/crypto/symmetric-algorithm) | Symmetric cipher algorithm identifiers supported by this package. |

## Variables

| Name | Summary |
| --- | --- |
| [`hkdf`](/api/crypto/hkdf) | HKDF (RFC 5869): derive keys from an initial input. |
| [`nacl`](/api/crypto/nacl) | — |

## Namespaces

- `nacl` — _export_
