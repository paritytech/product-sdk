---
generated: true
title: "xchachaDecryptPacked()"
description: "Decrypt a packed XChaCha20-Poly1305 buffer (nonce prepended to ciphertext)."
---
# `xchachaDecryptPacked()`

Decrypt a packed XChaCha20-Poly1305 buffer (nonce prepended to ciphertext).

Input format: `nonce(24) || ciphertext`.

```ts
xchachaDecryptPacked(packed: Uint8Array, key: Uint8Array): Uint8Array
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `packed` | `Uint8Array` | The packed buffer to decrypt. |
| `key` | `Uint8Array` | 32-byte encryption key. |

## Returns

`Uint8Array` — The decrypted plaintext.

## Throws

- If the packed data is too short, key is invalid, or authentication fails.
