---
generated: true
title: "randomBytes()"
description: "Generate `length` cryptographically secure random bytes."
---
# `randomBytes()`

Cryptographically secure PRNG. Uses internal OS-level `crypto.getRandomValues`.

```ts
randomBytes(bytesLength?: number): Uint8Array
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bytesLength` _(optional)_ | `number` | — |

## Returns

`Uint8Array`
