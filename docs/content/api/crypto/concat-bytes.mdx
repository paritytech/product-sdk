---
generated: true
title: "concatBytes()"
description: "Concatenate multiple `Uint8Array` instances into a single `Uint8Array`."
---
# `concatBytes()`

Copies several Uint8Arrays into one.

```ts
concatBytes(...arrays: Uint8Array<ArrayBufferLike>[]): Uint8Array
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `arrays` | `Uint8Array<ArrayBufferLike>[]` | — |

## Returns

`Uint8Array`
