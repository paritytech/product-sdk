---
generated: true
title: "interface ContractDefaults"
description: "Mutable defaults shared across all contract handles from a manager."
---
# `interface ContractDefaults`

Mutable defaults shared across all contract handles from a manager.

## Properties

### property `origin`

```ts
origin: SS58String
```

### property `signer`

```ts
signer: PolkadotSigner
```

### property `signerManager`

```ts
signerManager: SignerManager
```
