---
generated: true
title: "generateContractTypes()"
description: "Generate a TypeScript module augmentation that extends the"
---
# `generateContractTypes()`

Generate a TypeScript module augmentation that extends the
`Contracts` interface with typed method signatures for each
installed contract.

The output is written to `.cdm/contracts.d.ts` (or equivalent) and
augments `"@parity/product-sdk-contracts"` so that
`ContractManager.getContract()` returns fully-typed handles.

```ts
generateContractTypes(contracts: { abi: AbiEntry[]; library: string }[]): string
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `contracts` | `{ abi: AbiEntry[]; library: string }[]` | — |

## Returns

`string`

## Examples

```ts
const src = generateContractTypes([
    { library: "@example/counter", abi },
]);
writeFileSync(".cdm/contracts.d.ts", src);
```
