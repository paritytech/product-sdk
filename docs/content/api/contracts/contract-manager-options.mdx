---
generated: true
title: "interface ContractManagerOptions"
description: "Options for `ContractManager` construction."
---
# `interface ContractManagerOptions`

Options for `ContractManager` construction.

**Extends:** `ContractOptions`

## Properties

### property `targetHash`

```ts
targetHash: string
```

Explicit target hash to select from cdm.json. Defaults to the first target.
