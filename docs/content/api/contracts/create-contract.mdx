---
generated: true
title: "createContract()"
description: "Create a contract handle from a raw address and ABI — no `cdm.json` needed."
---
# `createContract()`

Create a contract handle from a raw address and ABI — no `cdm.json` needed.

```ts
createContract(inkSdk: InkSdk, address: HexString, abi: AbiEntry[], options?: ContractOptions): Contract<ContractDef>
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `inkSdk` | `InkSdk` | — |
| `address` | `HexString` | — |
| `abi` | `AbiEntry[]` | — |
| `options` _(optional)_ | `ContractOptions` | — |

## Returns

`Contract<ContractDef>`

## Examples

```ts
import { createInkSdk } from "@polkadot-api/sdk-ink";

const inkSdk = createInkSdk(client.raw.assetHub, { atBest: true });
const counter = createContract(inkSdk, "0xC472...", abi, {
    signerManager: signerManager,
});
await counter.getCount.query();
await counter.increment.tx();
```
