---
generated: true
title: "fetchBytes()"
description: "Fetch raw bytes from the gateway."
---
# `fetchBytes()`

Fetch raw bytes from the gateway.

```ts
fetchBytes(cid: string, gateway: string, options?: FetchOptions): Promise<Uint8Array<ArrayBufferLike>>
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `cid` | `string` | — |
| `gateway` | `string` | — |
| `options` _(optional)_ | `FetchOptions` | — |

## Returns

`Promise<Uint8Array<ArrayBufferLike>>`

## Throws

- If the gateway returns a non-OK response.
