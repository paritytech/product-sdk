---
generated: true
title: "cidExists()"
description: "Check if a CID exists on the gateway (HEAD request). Returns false on any error or timeout."
---
# `cidExists()`

Check if a CID exists on the gateway (HEAD request). Returns false on any error or timeout.

```ts
cidExists(cid: string, gateway: string, options?: FetchOptions): Promise<boolean>
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `cid` | `string` | — |
| `gateway` | `string` | — |
| `options` _(optional)_ | `FetchOptions` | — |

## Returns

`Promise<boolean>`
