---
generated: true
title: "gatewayUrl()"
description: "Build the full gateway URL for a CID."
---
# `gatewayUrl()`

Build the full gateway URL for a CID.

```ts
gatewayUrl(cid: string, gateway: string): string
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `cid` | `string` | — |
| `gateway` | `string` | — |

## Returns

`string`
