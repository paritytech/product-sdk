---
generated: true
title: "getGateway()"
description: "Get the IPFS gateway URL for an environment."
---
# `getGateway()`

Get the IPFS gateway URL for an environment.

```ts
getGateway(env: Environment): string
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `env` | `Environment` | — |

## Returns

`string`

## Throws

- If the network doesn't have a live gateway yet.
