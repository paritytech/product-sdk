---
generated: true
title: "computeCid()"
description: "Compute the CIDv1 (blake2b-256, raw codec) for arbitrary data."
---
# `computeCid()`

Compute the CIDv1 (blake2b-256, raw codec) for arbitrary data.
Deterministic: same input always produces the same CID.

```ts
computeCid(data: Uint8Array): string
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `Uint8Array` | — |

## Returns

`string`
