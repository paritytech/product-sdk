---
generated: true
title: "class BulletinLookupTimeoutError"
description: "The host preimage lookup timed out."
---
# `class BulletinLookupTimeoutError`

The host preimage lookup timed out.

The host was unable to retrieve the requested content within the timeout period.
The content may still become available later.

**Extends:** `BulletinError`

## Constructors

### constructor `constructor`

```ts
new BulletinLookupTimeoutError(cid: string, timeoutMs: number): BulletinLookupTimeoutError
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `cid` | `string` | — |
| `timeoutMs` | `number` | — |

## Properties

### property `cid`

```ts
cid: string
```

The CID that was being looked up.

### property `timeoutMs`

```ts
timeoutMs: number
```

The timeout duration in milliseconds.
