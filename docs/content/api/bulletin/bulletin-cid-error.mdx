---
generated: true
title: "class BulletinCidError"
description: "Invalid CID format or version."
---
# `class BulletinCidError`

Invalid CID format or version.

Thrown when a CID string cannot be parsed or has an unexpected version/codec.

**Extends:** `BulletinError`

## Constructors

### constructor `constructor`

```ts
new BulletinCidError(message: string, cid?: string): BulletinCidError
```

#### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `message` | `string` | — |
| `cid` _(optional)_ | `string` | — |

## Properties

### property `cid`

```ts
cid: string
```

The invalid CID string, if available.
