---
generated: true
title: "utf8ToBytes()"
description: "Encode a UTF-8 string into a `Uint8Array`."
---
# `utf8ToBytes()`

Converts string to bytes using UTF8 encoding.

```ts
utf8ToBytes(str: string): Uint8Array
```

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `str` | `string` | — |

## Returns

`Uint8Array`

## Examples

```ts
utf8ToBytes('abc') // Uint8Array.from([97, 98, 99])
```
