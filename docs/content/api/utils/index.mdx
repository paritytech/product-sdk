---
generated: true
title: "@parity/product-sdk-utils"
description: "Encoding, hashing, token formatting, and balance querying for the Polkadot app ecosystem."
---

# `@parity/product-sdk-utils`

Encoding, hashing, token formatting, and balance querying for the Polkadot app ecosystem.

Provides general-purpose byte encoding/decoding (`bytesToHex`, `hexToBytes`, `utf8ToBytes`,
`concatBytes`), 32-byte hash functions (`blake2b256`, `sha256`, `keccak256`),
Substrate token formatting (`formatPlanck`, `parseToPlanck`, `formatBalance`),
and typed balance queries (`getBalance`).
All functions are framework-agnostic.

```sh npm2yarn
npm install @parity/product-sdk-utils
```

## Functions

| Name | Summary |
| --- | --- |
| [`blake2b256()`](/api/utils/blake2b256) | Compute a 32-byte BLAKE2b-256 hash. |
| [`bytesToHex()`](/api/utils/bytes-to-hex) | Convert a `Uint8Array` to its lowercase hexadecimal string representation. |
| [`concatBytes()`](/api/utils/concat-bytes) | Concatenate multiple `Uint8Array` instances into a single `Uint8Array`. |
| [`formatBalance()`](/api/utils/format-balance) | Format a planck value for display with locale-aware thousand separators, |
| [`formatPlanck()`](/api/utils/format-planck) | Convert a planck (smallest indivisible token unit) value to a human-readable decimal string. |
| [`getBalance()`](/api/utils/get-balance) | Query the free, reserved, and frozen balances for an on-chain address. |
| [`hexToBytes()`](/api/utils/hex-to-bytes) | Decode a hexadecimal string into a `Uint8Array`. |
| [`keccak256()`](/api/utils/keccak256) | Compute a 32-byte Keccak-256 hash. |
| [`parseToPlanck()`](/api/utils/parse-to-planck) | Parse a human-readable decimal token amount into its planck (smallest unit) representation. |
| [`sha256()`](/api/utils/sha256) | Compute a 32-byte SHA2-256 hash. |
| [`utf8ToBytes()`](/api/utils/utf8-to-bytes) | Encode a UTF-8 string into a `Uint8Array`. |

## Interfaces

| Name | Summary |
| --- | --- |
| [`AccountBalance`](/api/utils/account-balance) | Balance breakdown from a Substrate `System.Account` query. |
| [`BalanceApi`](/api/utils/balance-api) | Minimal structural type for a PAPI typed API with `System.Account`. |
| [`FormatBalanceOptions`](/api/utils/format-balance-options) | Options for `formatBalance`. |
