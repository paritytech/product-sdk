---
generated: true
title: "interface BalanceApi"
description: "Minimal structural type for a PAPI typed API with `System.Account`."
---
# `interface BalanceApi`

Minimal structural type for a PAPI typed API with `System.Account`.

Structural so it works with any chain that has the System pallet, without
importing chain-specific descriptors.

## Properties

### property `query`

```ts
query: { System: { Account: { getValue: unknown } } }
```
