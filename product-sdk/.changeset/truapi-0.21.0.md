---
"@parity/product-sdk": minor
"@parity/product-sdk-host": minor
"@parity/product-sdk-renderer": minor
---

**Pair with a truapi 0.21.0 host.** `@parity/truapi` moves from `^0.20.0` to `^0.21.0`. The codec version (3) and `TRUAPI_WIRE_SCHEMA_HASH` (`462dacb6e0d1f504`) are unchanged, so products and hosts on either version still talk to each other.
