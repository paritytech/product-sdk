packages:
  - "packages/*"
  - "examples/*"
  - "scripts/bench-consumer"

catalog:
  "@parity/host-api-test-sdk": ^0.11.0
  "@parity/truapi": ^0.7.0
  "@playwright/test": ^1.60.0
  "@polkadot-api/json-rpc-provider": ^0.2.0
  "@polkadot-api/substrate-bindings": ^0.20.3
  "@polkadot-api/substrate-client": ^0.7.0
  "@polkadot-labs/hdkd-helpers": ^0.0.30
  polkadot-api: ^2.1.6
  tsup: ^8.5.1
  typescript: ^5.9.3
  viem: ^2.52.0
  vite: ^6.4.3
  vitest: ^3.1.4

overrides:
  "@polkadot-api/json-rpc-provider": "^0.2.0"
  # Force the patched `ws` (GHSA: memory-exhaustion DoS from tiny fragments;
  # affects >=8.0.0 <8.21.0). Pulled transitively by smoldot (`^8.8.1`, satisfied)
  # and viem (pinned `8.20.1`, overridden). All major-8, so a patched minor.
  "ws": "^8.21.0"

onlyBuiltDependencies:
  - "@biomejs/biome"
  - "esbuild"

allowBuilds:
  "@biomejs/biome": true
  esbuild: true
